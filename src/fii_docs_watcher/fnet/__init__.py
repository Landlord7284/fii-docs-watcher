"""Client for the Fundos.NET public endpoints.

This is scraping of a UI endpoint, not a contracted API: no versioning, no SLA,
no stability guarantee. Everything encoded here was verified empirically and
carries a note about how it was verified, so a future maintainer can re-check
rather than trust.
"""
